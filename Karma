-- █▀▀ █▀▀█ █▀▀▄ █▀▀ ▀▀█▀▀ █▀▀ █▀▀█ █▀▀▄ 
-- █░░ █▄▄█ █▀▀▄ █▀▀ ░░█░░ █▀▀ █▄▄█ █▀▀▄ 
-- ▀▀▀ ▀░░▀ ▀▀▀░ ▀▀▀ ░░▀░░ ▀▀▀ ▀░░▀ ▀▀▀░ 
-- ПОПУЛЯРНЫЕ ЧИТЕРСКИЕ ФИШКИ + RAYFIELD UI

local Rayfield = loadstring(game:HttpGet('https://sirius.menu/rayfield'))()
local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer
local RunService = game:GetService("RunService")
local Workspace = game:GetService("Workspace")

local Window = Rayfield:CreateWindow({
    Name = "🔥 MM2 POPULAR CHEAT PACK",
    LoadingTitle = "Загрузка популярных функций...",
    LoadingSubtitle = "Самые востребованные читы",
    ConfigurationSaving = { Enabled = true },
    KeySystem = false
})

-- 🎯 САМЫЕ ПОПУЛЯРНЫЕ ФУНКЦИИ
local PopularCheats = {
    ESP = false,
    AimBot = false,
    NoClip = false,
    SpeedHack = false,
    Fly = false,
    InfiniteJump = false,
    AntiAfk = true,
    AutoFarm = false,
    KillAura = false,
    SilentAim = false
}

-- 👁️ ПОПУЛЯРНЫЙ ESP
local function ToggleESP()
    for _, player in pairs(Players:GetPlayers()) do
        if player ~= LocalPlayer and player.Character then
            local highlight = player.Character:FindFirstChild("ESP_Highlight")
            
            if PopularCheats.ESP then
                if not highlight then
                    highlight = Instance.new("Highlight")
                    highlight.Name = "ESP_Highlight"
                    highlight.FillColor = Color3.new(1, 0, 0)
                    highlight.OutlineColor = Color3.new(1, 1, 1)
                    highlight.FillTransparency = 0.5
                    highlight.Parent = player.Character
                end
            else
                if highlight then
                    highlight:Destroy()
                end
            end
        end
    end
end

-- 🎯 AIMBOT (самый популярный)
local function AimAtPlayer(target)
    if target and target.Character then
        local head = target.Character:FindFirstChild("Head")
        if head then
            Workspace.CurrentCamera.CFrame = CFrame.new(
                Workspace.CurrentCamera.CFrame.Position,
                head.Position
            )
        end
    end
end

-- 🚀 NO-CLIP (очень популярно)
local function NoClip()
    if PopularCheats.NoClip and LocalPlayer.Character then
        for _, part in pairs(LocalPlayer.Character:GetDescendants()) do
            if part:IsA("BasePart") then
                part.CanCollide = false
            end
        end
    end
end

-- 💨 SPEED HACK
local function SpeedHack()
    if PopularCheats.SpeedHack and LocalPlayer.Character then
        LocalPlayer.Character.Humanoid.WalkSpeed = 50
    else
        LocalPlayer.Character.Humanoid.WalkSpeed = 16
    end
end

-- 🕊️ FLY HACK
local function FlyHack()
    if PopularCheats.Fly and LocalPlayer.Character then
        local root = LocalPlayer.Character.HumanoidRootPart
        local bodyVelocity = root:FindFirstChild("FlyVelocity") or Instance.new("BodyVelocity")
        bodyVelocity.Name = "FlyVelocity"
        bodyVelocity.Velocity = Vector3.new(0, 0, 0)
        bodyVelocity.MaxForce = Vector3.new(40000, 40000, 40000)
        bodyVelocity.Parent = root
        
        -- Управление полетом
        local camera = Workspace.CurrentCamera
        local moveDirection = Vector3.new(0, 0, 0)
        
        if game:GetService("UserInputService"):IsKeyDown(Enum.KeyCode.W) then
            moveDirection = moveDirection + camera.CFrame.LookVector
        end
        if game:GetService("UserInputService"):IsKeyDown(Enum.KeyCode.S) then
            moveDirection = moveDirection - camera.CFrame.LookVector
        end
        if game:GetService("UserInputService"):IsKeyDown(Enum.KeyCode.A) then
            moveDirection = moveDirection - camera.CFrame.RightVector
        end
        if game:GetService("UserInputService"):IsKeyDown(Enum.KeyCode.D) then
            moveDirection = moveDirection + camera.CFrame.RightVector
        end
        if game:GetService("UserInputService"):IsKeyDown(Enum.KeyCode.Space) then
            moveDirection = moveDirection + Vector3.new(0, 1, 0)
        end
        if game:GetService("UserInputService"):IsKeyDown(Enum.KeyCode.LeftShift) then
            moveDirection = moveDirection + Vector3.new(0, -1, 0)
        end
        
        bodyVelocity.Velocity = moveDirection * 50
    else
        local velocity = LocalPlayer.Character.HumanoidRootPart:FindFirstChild("FlyVelocity")
        if velocity then
            velocity:Destroy()
        end
    end
end

-- 🦘 INFINITE JUMP
local function InfiniteJump()
    game:GetService("UserInputService").JumpRequest:connect(function()
        if PopularCheats.InfiniteJump then
            LocalPlayer.Character:FindFirstChildOfClass("Humanoid"):ChangeState("Jumping")
        end
    end)
end

-- ⚔️ KILL AURA (популярно в PVP)
local function KillAura()
    if PopularCheats.KillAura then
        for _, player in pairs(Players:GetPlayers()) do
            if player ~= LocalPlayer and player.Character then
                local distance = (LocalPlayer.Character.HumanoidRootPart.Position - 
                                player.Character.HumanoidRootPart.Position).Magnitude
                if distance < 10 then
                    LocalPlayer.Character.HumanoidRootPart.CFrame = 
                        player.Character.HumanoidRootPart.CFrame
                    wait(0.1)
                end
            end
        end
    end
end

-- 🎯 SILENT AIM
local function SilentAim()
    if PopularCheats.SilentAim then
        -- Автоматическое прицеливание при стрельбе
        local closestPlayer = nil
        local closestDistance = math.huge
        
        for _, player in pairs(Players:GetPlayers()) do
            if player ~= LocalPlayer and player.Character then
                local distance = (LocalPlayer.Character.HumanoidRootPart.Position - 
                                player.Character.HumanoidRootPart.Position).Magnitude
                if distance < closestDistance then
                    closestDistance = distance
                    closestPlayer = player
                end
            end
        end
        
        if closestPlayer then
            AimAtPlayer(closestPlayer)
        end
    end
end

-- 💰 AUTO FARM
local function AutoFarm()
    if PopularCheats.AutoFarm then
        -- Поиск монет или шаров
        for _, obj in pairs(Workspace:GetChildren()) do
            if obj.Name:lower():find("coin") or obj.Name:lower():find("ball") then
                if LocalPlayer.Character then
                    LocalPlayer.Character.HumanoidRootPart.CFrame = obj.CFrame
                    wait(0.5)
                end
            end
        end
    end
end

-- 🎮 ГЛАВНАЯ ВКЛАДКА
local MainTab = Window:CreateTab("🔥 Популярные читы", 4483362458)

-- 👁️ СЕКЦИЯ ВИЗУАЛА
local VisualSection = MainTab:CreateSection("👁️ Визуальные читы")

MainTab:CreateToggle({
    Name = "👁️ ESP (видеть через стены)",
    CurrentValue = false,
    Callback = function(Value)
        PopularCheats.ESP = Value
        ToggleESP()
    end
})

MainTab:CreateToggle({
    Name = "🎯 AimBot (авто-прицеливание)",
    CurrentValue = false,
    Callback = function(Value)
        PopularCheats.AimBot = Value
    end
})

MainTab:CreateToggle({
    Name = "🔫 Silent Aim (скрытное прицеливание)",
    CurrentValue = false,
    Callback = function(Value)
        PopularCheats.SilentAim = Value
    end
})

-- 🚀 СЕКЦИЯ ДВИЖЕНИЯ
local MovementSection = MainTab:CreateSection("🚀 Читы движения")

MainTab:CreateToggle({
    Name = "👻 NoClip (сквозь стены)",
    CurrentValue = false,
    Callback = function(Value)
        PopularCheats.NoClip = Value
    end
})

MainTab:CreateToggle({
    Name = "💨 Speed Hack (50 speed)",
    CurrentValue = false,
    Callback = function(Value)
        PopularCheats.SpeedHack = Value
        SpeedHack()
    end
})

MainTab:CreateToggle({
    Name = "🕊️ Fly Hack (полет)",
    CurrentValue = false,
    Callback = function(Value)
        PopularCheats.Fly = Value
    end
})

MainTab:CreateToggle({
    Name = "🦘 Infinite Jump",
    CurrentValue = false,
    Callback = function(Value)
        PopularCheats.InfiniteJump = Value
        if Value then InfiniteJump() end
    end
})

-- ⚔️ СЕКЦИЯ БОЯ
local CombatSection = MainTab:CreateSection("⚔️ Боевые читы")

MainTab:CreateToggle({
    Name = "💀 Kill Aura (авто-атака)",
    CurrentValue = false,
    Callback = function(Value)
        PopularCheats.KillAura = Value
    end
})

MainTab:CreateButton({
    Name = "⚡ Kill All (убить всех)",
    Callback = function()
        for _, player in pairs(Players:GetPlayers()) do
            if player ~= LocalPlayer then
                LocalPlayer.Character.HumanoidRootPart.CFrame = 
                    player.Character.HumanoidRootPart.CFrame
                wait(0.2)
            end
        end
    end
})

-- 🤖 СЕКЦИЯ АВТОМАТИЗАЦИИ
local AutoSection = MainTab:CreateSection("🤖 Автоматизация")

MainTab:CreateToggle({
    Name = "💰 Auto Farm (авто-сбор)",
    CurrentValue = false,
    Callback = function(Value)
        PopularCheats.AutoFarm = Value
    end
})

MainTab:CreateToggle({
    Name = "⏰ Anti-AFK (анти-афк)",
    CurrentValue = true,
    Callback = function(Value)
        PopularCheats.AntiAfk = Value
    end
})

MainTab:CreateButton({
    Name = "🔫 Grab Gun (взять пушку)",
    Callback = function()
        local gun = Workspace:FindFirstChild("GunDrop")
        if gun then
            LocalPlayer.Character.HumanoidRootPart.CFrame = gun.CFrame
        end
    end
})

-- 🔧 УТИЛИТЫ
local UtilTab = Window:CreateTab("🔧 Утилиты", 7733912645)

UtilTab:CreateButton({
    Name = "🌀 Shake All (шатать всех)",
    Callback = function()
        for _, player in pairs(Players:GetPlayers()) do
            if player ~= LocalPlayer and player.Character then
                local root = player.Character.HumanoidRootPart
                root.Velocity = Vector3.new(
                    math.random(-100, 100),
                    math.random(50, 100),
                    math.random(-100, 100)
                )
            end
        end
    end
})

UtilTab:CreateButton({
    Name = "🎯 Teleport to Murderer",
    Callback = function()
        for _, player in pairs(Players:GetPlayers()) do
            if player ~= LocalPlayer then
                local backpack = player:FindFirstChild("Backpack")
                if backpack then
                    for _, tool in pairs(backpack:GetChildren()) do
                        if tool:IsA("Tool") and string.lower(tool.Name):find("knife") then
                            LocalPlayer.Character.HumanoidRootPart.CFrame = 
                                player.Character.HumanoidRootPart.CFrame
                            return
                        end
                    end
                end
            end
        end
    end
})

-- 🔄 ОСНОВНОЙ ЦИКЛ
RunService.Heartbeat:Connect(function()
    -- NoClip
    if PopularCheats.NoClip then
        NoClip()
    end
    
    -- Fly Hack
    if PopularCheats.Fly then
        FlyHack()
    end
    
    -- Kill Aura
    if PopularCheats.KillAura then
        KillAura()
    end
    
    -- Silent Aim
    if PopularCheats.SilentAim then
        SilentAim()
    end
    
    -- Auto Farm
    if PopularCheats.AutoFarm then
        AutoFarm()
    end
    
    -- ESP Update
    if PopularCheats.ESP then
        ToggleESP()
    end
end)

-- ⏰ ANTI-AFK SYSTEM
if PopularCheats.AntiAfk then
    local VirtualUser = game:GetService("VirtualUser")
    game:GetService("Players").LocalPlayer.Idled:connect(function()
        VirtualUser:CaptureController()
        VirtualUser:ClickButton2(Vector2.new())
    end)
end

-- 🎉 УВЕДОМЛЕНИЕ О ЗАГРУЗКЕ
Rayfield:Notify({
    Title = "🔥 POPULAR CHEAT PACK LOADED!",
    Content = "Самые популярные читы активированы!",
    Duration = 6,
})

print("🎮 Самые популярные читы для MM2 загружены!")
print("✅ Все функции готовы к использованию!")
